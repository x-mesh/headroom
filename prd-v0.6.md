# Rack Mesh PRD v0.6 — 근거를 나르는 결과물

작성일: 2026-09-07
개정: v0.6 (방향 판정, 종료 게이트, Phase 1 잔여 요구사항)
상태: 초안 (Draft) · 차단 결정 4건 반영
기준 코드: `develop` `626de48`
근거: 코드·PRD·시장을 여섯 갈래로 읽고 네 방향 논제를 세 렌즈로 채점한 뒤, 여덟 관점의 장치 후보를 후보마다 세 검증자가 반박한 결과

이 문서는 `infra-simulator-prd.md`(v0.5)를 대체하지 않는다. v0.5의 개념 모델(5절), 데이터 모델(7절), 벤더 원칙(8절)은 그대로 유효하다. 이 개정은 **무엇을 먼저 만들고 무엇을 만들지 않는지**를 정하고, Phase 1에 남은 요구사항에 ID를 붙인다.

---

## 1. 이 개정이 정하는 것

v0.5는 세 가지 쓰임(인프라 교육, 살아있는 아키텍처 다이어그램, 용량 산정)을 모두 열어 두었다. 코드가 그 셋을 동시에 향하면서 무엇이 본체인지 정하지 않은 상태로 73커밋이 쌓였고, 그중 8커밋이 아이콘 반복, 45커밋이 리뷰 스크린샷 갱신이었다. 이 개정은 순서를 정한다.

동시에 v0.5가 세운 원칙과 코드 사이의 격차 두 곳을 요구사항으로 못박는다. 하나는 화면이 미확인 값을 숫자로 지어내던 자리이고, 다른 하나는 "근거 있는 산정"이라는 주장을 카탈로그가 아직 못 받치는 자리다. 두 번째 격차는 원칙을 어겨서 생긴 것이 아니라 원칙을 지킨 대가로 생겼다. 2절이 그 구조를 적는다.

---

## 2. 방향 판정

**본체는 용량 산정이다.** 설계 리뷰에 첨부할 계산 근거를 결정적으로 내는 엔진이 제품이다. **살아있는 다이어그램은 그 근거를 읽고 what-if를 묻는 표면**이고, **교육 템플릿은 그 근거의 사례집**이다. 버리는 방향은 없고 순서만 정한다.

세 심판(사용자 가치 · 실현성 · 차별성)이 모두 용량 산정을 1위에 두었다. **2위는 갈렸다.** 둘은 살아있는 다이어그램을, 실현성 심판은 교육을 2위로 봤다. 이 개정은 2위를 확정하지 않는다. 5절의 우선순위는 본체를 뒷받침하는 순서이지 2·3위 판정이 아니다.

**반대 논제가 맞은 것.** "근거 있는 산정"은 아직 코드가 뒷받침하지 못하는 주장이다. 리뷰에 첨부할 것이 `createExport` JSON 하나뿐이고, 카탈로그 26종 75프로필에 구조화된 `conditions`가 0건이다. 실패하면 중단하는 게이트, 기존 도면 입력 없이는 draw.io를 못 이긴다는 지적을 채택한다.

**그 0건이 지금 만들어 내는 상태.** `evidenceApplicability`는 `record.conditions`가 없으면 `unknown`을 돌려주고(`src/evidence.js:44`), 엔진은 그 축을 `unknownReason: 'evidence-applicability-unknown'` 으로 내린다(`src/engine.js:443-453`). 그래서 카탈로그 장비를 붙이면 **한계값을 알면서도 사용률을 계산하지 않는다.**

```
FortiGate 100F · fw-1518 프로필을 붙인 뒤
limits: forwarding_bps=20000000000 · new_sessions_per_sec=56000 · ...
records conditions: 네 축 모두 null
  forwarding_bps        status=unknown  evidenceApplicability=unknown  utilization=null
  new_sessions_per_sec  status=unknown  evidenceApplicability=unknown  utilization=null
```

반면 템플릿 장비는 `spec`이 없어 판정을 아예 거치지 않으므로(`src/engine.js:442` `if (!record) continue;`) 정상 계산된다. **출처 없는 합성값은 계산되고 출처 있는 데이터시트 값은 계산되지 않는다.** 원칙("조건을 모르는 값을 자동 적용하지 않는다", v0.5 7.2)이 뒤집힌 것이 아니라 정확히 그대로 작동한 결과다. 대가가 카탈로그 전체를 계산에 못 쓰게 만드는 것뿐이다.

**우회는 화면 쪽에 있었다.** 이 절의 앞선 판은 `conditionSelection: 'explicit-profile'` 우회가 카탈로그에 걸리지 않는다고 적었는데 틀렸다. 엔진 경로에서는 걸리지 않지만, 인스펙터의 카탈로그 드롭다운이 프로필을 붙일 때 그 플래그를 함께 달아 판정을 통째로 건너뛰고 있었다. 프로필을 고른 것을 조건을 확인한 것과 같다고 친 셈이다. 5.7절이 그 대역을 걷어냈다. 남는 것은 `src/device-import.js`가 외부 정의를 명시적 조건으로 임포트하는 경로 하나이며, 그 값은 `applicable`이 아니라 `user-asserted`로 표기한다.

**반대 논제가 틀린 것.** 세 방향이 양립 불가라는 전제다. 계산 계층(`src/engine.js`)과 표현 계층(`src/app.js`)이 이미 분리되어 있어 이것은 우선순위 문제다.

**이 판정이 틀렸다고 판명될 신호.**

1. 90일 안에 `src/report.js`와 축 단위 `conditions` 30종이 서지 않는다.
2. 보고서를 받은 리뷰어가 문서 안에서 장애를 껐다 켜기를 반복 요구한다. 그러면 살아있는 다이어그램이 본체다.
3. 리뷰 첨부는 0건인데 강사·온보딩 기록만 쌓인다. 그러면 교육이 본체다.

**시장 대조.** WAN 용량 계획기(Cisco WAE, Juniper Paragon Planner)는 장애 집합별 링크 사용률과 N-1 최악 사용률을 지도 위에 이미 그린다(검증자 진술, 원문 미확인). 다만 bps 한 축이다. 이 도구가 다른 점은 축 아홉 개와 상한·미확인 표시를 함께 나르는 것뿐이다. 오버레이가 축·상한·원인을 빼고 색만 바꾸면 따라하기다. 5절 요구사항은 모두 축과 근거를 함께 나르도록 인수 기준을 건다.

---

## 3. 이 개정의 목표와 비목표

**목표.**

- 계산 결과를 설계 리뷰 문서에 붙일 수 있는 형태로 낸다. 그림 한 장에도 배율, 장애, 엔진 버전, 병목 축, 미확인이 함께 실린다.
- 카탈로그 장비를 붙였을 때 계산이 돌게 한다. 지금은 데이터시트 장비를 붙이면 모든 축이 미확인으로 떨어져, 합성 템플릿보다 답을 못 한다.
- 엔진이 이미 계산한 값 중 화면이 읽지 않는 것을 읽는다. 새 모델을 만들기 전에 있는 것을 쓴다.

**이 개정의 비목표.** v0.5 3절의 비목표에 더한다.

- 시간축과 재생. Phase 2 영역이며, 정상 상태 두 장의 비교로 답할 수 있는 질문을 먼저 소진한다.
- 문서 임베딩(URL 주입, iframe, postMessage). 보고서가 실제 리뷰에 붙은 뒤 다시 올린다. 6.1 참조.
- 실측 잔차 병기와 MAPE 산출. 조건이 구조화되기 전에는 비교 집단이 성립하지 않는다.

---

## 4. 종료 게이트

**게이트는 코드가 도달한 상태로만 판정한다.** 외부 팀 사용 사례와 축별 오차는 90일 안에 확인할 근거가 없다. 90일 안에 판정할 수 없는 조건은 게이트가 아니다.

| # | 게이트 | 측정 방법 | 크기 | 주 |
|---|---|---|---|---|
| G1 | 카탈로그 판 식별자 왕복 | 프로젝트에 `catalogRevision`을 넣고 저장·열기 왕복 후 값이 보존되며, 형식이 다르면 거절한다. `createExport`에 같은 값이 실린다 | S | 1–4 |
| G2 | 카탈로그 축의 판정 가능률 80% | 측정 함수는 **완료** — `summary.evidenceJudgement`가 `spec.records`를 가진 축 중 판정이 `unknown`이 **아닌** 비율을 내고 `createExport`에 실린다. 남은 것은 그 값을 0.8 이상으로 올리는 일이며, 데이터시트가 조건을 밝히지 않은 축(`unknown`)이 얼마나 되는지가 상한을 정한다 | M | 3–10 |
| G3 | 축 레코드 30개에 측정 조건 | **도달.** 방화벽 카탈로그의 축 레코드 65개가 각자 `conditions`를 갖고, `buildSpec` 왕복 후 축마다 다른 값을 유지한다 | M | 3–10 |
| G4 | 보고서 세 형식 스냅샷 | `src/report.js`가 보고서 모델 하나에서 마크다운·HTML·JSON 셋을 내고, 셋 다 골든 시나리오 스냅샷과 일치한다. 어느 형식에서든 미확인 축이 숫자로 나오면 실패한다 | M | 9–12 |

**게이트가 아닌 것.** 서로 다른 세 팀의 설계 리뷰 첨부, 조건 일치 실측 10건, 축별 MAPE. 이 셋은 `docs/pilot-log.md`에 기록만 시작하고 다음 분기 지표로 둔다. v0.5 14절의 Beta 지표는 그대로 유효하다.

**G2를 적용 일치율이 아니라 판정 가능률로 재는 이유.** 비교가 정확 동등이라(Q3 결정) 실무 워크로드와 데이터시트 조건이 그대로 맞는 경우는 드물다. `applicable` 비율을 목표로 걸면 처음부터 도달할 수 없는 게이트가 된다. 이 도구가 못 하면 안 되는 일은 "맞다고 말하는 것"이 아니라 **"맞는지 아닌지 말하는 것"** 이다. `incompatible`은 판정에 성공한 것이고 `unknown`만 실패다.

---

## 5. 기능 요구사항

Phase 1의 연속이다. ID는 v0.5의 P1-15에 이어 붙인다. 인수 기준은 테스트로 확인할 수 있는 문장으로 쓴다.

### 5.1 첨부할 수 있는 결과물

| ID | 요구사항 | 인수 기준 |
|---|---|---|
| P1-16 | 내보낸 그림에 계산 결과와 판정을 함께 찍는다 | `exportDiagramSvg(topology, result, options)`가 배율, 장애 목록, 엔진 버전, 합성 여부, 평가 상태, 최소 headroom과 그 자원·축, 내보낸 시각을 담는다. 미확인 축에 백분율과 채워진 막대가 나오면 실패한다. 결과 없이 부르면 도면만 나오고 계산 결과 없음을 적는다 |
| P1-17 | 결과 필드만으로 "왜 이 숫자인가"를 문장으로 낸다 | 순수 함수가 결과와 기준선만 받아 문장을 낸다. `load / limit`을 직접 나누지 않는다. 미확인이 있으면 상한 문장이 첫 줄이다. 골든 시나리오 스냅샷으로 결정성을 고정한다 |
| P1-18 | 보고서를 세 형식으로 낸다 | `src/report.js`가 결과에서 **보고서 모델**(문자열이 아닌 구조)을 만들고, 마크다운·HTML·JSON 렌더러가 그 모델만 읽는다. 세 스냅샷이 같은 사실을 담는다. 한계값마다 출처와 적용 판정이 붙고, 명시 수락분은 `user-asserted`로 표기한다. 숫자를 만드는 곳은 모델 하나뿐이다 |
| P1-19 | 저장한 결과가 어느 카탈로그 판에서 나왔는지 말한다 | `catalogRevision`이 프로젝트 왕복에서 보존되고 형식 검증을 받는다. 장비별 `spec.revision`이 없으면 단일 날짜를 지어내지 않고 미확인으로 둔다 |

### 5.2 계산이 이미 아는 것을 화면에 낸다

새 모델을 만들지 않는다. 엔진 결과에 이미 있으나 화면이 읽지 않는 값을 읽는다.

| ID | 요구사항 | 인수 기준 |
|---|---|---|
| P1-20 | 축별 돌파 배율을 순서대로 보인다 | `summary.growthLadder`가 축마다 한계를 넘는 배율과 경고 배율을 낸다. 이분 탐색과 같은 답을 낸다(모든 템플릿, 세 배율, 오차 0.011 이내). 한계를 모르는 축은 순서에 넣지 않고 별도로 센다. 모델명 `linear-offered-load`를 `createExport`에 밝힌다 |
| P1-21 | 페일오버 서지를 정상 부하와 나눠 보인다 | 축 미터가 정상분과 재수립분을 나눠 쌓고 산식을 한 줄로 적는다. 재수립 창이나 동시 세션을 모르면 미확인으로 표시하고 사유를 구분한다 |
| P1-22 | 기준선 대비 변화를 자원·축·방향별로 보인다 | 기준선과 현재를 id·축·방향으로 대조한다. 링크는 평면 축이 아니라 방향별로 대조한다. 한쪽이 미확인이면 숫자를 만들지 않는다. 기준선 이후 설계가 바뀌었으면 숫자 대신 그 사실을 적는다 |
| P1-23 | demand 하나의 경로와 몫을 보인다 | 선택한 demand의 경로, 홉 갈래 몫, 조임점과 그 방향, 장애 이전 경로를 그린다. 경로 열거가 상한에서 잘렸으면 홉 라벨을 숨기고 그 사실을 적는다. 화면 조작으로 시나리오를 다시 계산하지 않는다 |
| P1-24 | 단일 장애 최악 사용률을 자원·축별로 보인다 | 자원 하나씩 끄는 훑기가 자원·축별 최악 사용률과 원인 자원 id를 낸다. 장애 케이스에서 그 축이 미확인이거나 전달률이 상한이면 상한으로 표시하고 정상 색으로 내리지 않는다. 훑기 결과 전체를 메모리에 보관하지 않는다 |
| P1-25 | 이름 붙인 시나리오를 한 표로 판정한다 | 순수 함수가 시나리오를 순서대로 계산해 서비스별 판정(통과·실패·미확인·오류)을 낸다. 단일 백분율로 합치지 않는다. 순서를 인과로 표기하지 않는다 |

P1-24와 P1-25는 게이트 이후 항목이다. 나머지는 게이트와 병행한다.

### 5.3 근거의 적용 가능성

v0.5 7.2절이 요구한 측정 조건이 코드에 0건이고, 그 결과 카탈로그 장비가 계산에서 빠진다(2절). 이 절이 그 격차를 메운다. 조건을 채우는 것만으로는 부족하다 — 대조할 워크로드 조건이 없으면 판정은 그대로 미확인이고, 조건이 맞지 않으면 계산이 다시 멈춘다. 그래서 P1-26·P1-27·P1-39가 한 릴리스에 함께 들어간다.

| ID | 요구사항 | 인수 기준 |
|---|---|---|
| P1-26 | 워크로드 조건을 사용자가 적는다 | **완료.** 툴바의 워크로드 조건 폼이 v0.5 7.2 어휘로 프레임 크기·전송 계층·암호 스위트·시험 방법·켜 둔 기능을 받는다. '기능 없음'과 '적지 않음'을 구분한다 — 빈 목록은 방화벽만 켠 프로필과 맞출 수 있는 값이고, 적지 않음은 대조할 수 없다는 뜻이다. `validateProject`가 키·길이·마크업을 검사한다 |
| P1-27 | 측정 조건을 프로필이 아니라 축에 붙인다 | **완료.** `buildSpec`이 `profile.axisConditions[axis]`를 먼저 읽는다. 방화벽 카탈로그의 축 레코드 65개가 조건을 갖는다(G3 목표 30). 조건 값은 구체값·`unknown`·`not_applicable` 셋이며 `unknown`은 불일치가 아니라 미확인으로, `not_applicable`은 대조 없이 통과로 읽는다 |
| P1-39 | 조건 불일치를 축 단위로 수락한다 | **완료.** 인스펙터의 근거 줄마다 '이 축만 수락' 버튼이 있다. 수락은 근거 digest와 그때의 워크로드 조건을 함께 해시한 값으로 저장되어, 조건이나 프로필이 바뀌면 자동으로 풀린다. 프로필을 통째로 수락하는 버튼은 없다. 이미 조건이 맞는 축과 데이터시트가 값을 적지 않은 축은 수락 대상이 아니다 |

### 5.4 기존 자산 입력

| ID | 요구사항 | 인수 기준 |
|---|---|---|
| P1-29 | 압축된 drawio 도면을 가져온다 | 압축 도면을 풀어 기존 파서로 넘긴다. 브라우저가 압축 해제를 지원하지 않으면 기존 오류 문구를 유지한다. 계산 대상이 아닌 장식 도형을 화면이 구분해 표시한다 |
| P1-30 | NetBox 인스턴스와 케이블을 가져온다 | 인터페이스를 포트로 만들고 케이블을 링크로 만든다. 원본 저장소와 commit을 provenance로 기록한다(v0.5 P1-11). 매핑표에 없는 타입을 조용히 버리지 않고 항목별로 보고한다 |

### 5.5 이번 개정에서 이미 반영한 것

`34e7276`에서 다음을 넣었다. 요구사항으로 남기는 것은 재발을 막기 위해서다.

| ID | 요구사항 | 상태 |
|---|---|---|
| P1-31 | 화면이 미확인 값을 숫자로 지어내지 않는다 | 완료. 최소 headroom이 미확인이면 두 스파크라인이 선을 그리지 않고, 그려 둔 선과 이력을 지운다 |
| P1-32 | 페일오버 서지의 미확인 입력을 구분해 말한다 | 완료. 동시 세션 미선언과 재수립 창 미선언을 서로 다른 사유로 기록한다. 선언된 0과 경로가 겹치지 않은 경우는 아는 0으로 둔다 |
| P1-33 | 화면 표기가 열려 있는 설계와 엔진에서 온다 | 완료. 제목, 합성 여부, 계산 노트, 엔진 버전이 하드코딩이 아니다 |
| P1-34 | 재계산 비용을 상호작용 종류에 맞춘다 | 완료. 배율을 끄는 동안은 훑기와 자동 저장을 건너뛰고, 손을 떼면 전체 경로가 돈다 |

### 5.6 적대적 검토가 되돌린 것

P1-31~P1-34를 넣은 뒤 같은 변경을 여섯 렌즈로 반박했다. 27건 중 10건이 두 검증자 모두에게 확정됐고, 그중 하나는 이번 변경이 만든 회귀였다. 아래 넷은 그 결과로 요구사항이 됐다.

| ID | 요구사항 | 인수 기준 |
|---|---|---|
| P1-35 | 한계에 정확히 닿은 축을 성장 순서에서 건너뛰지 않는다 | 초과가 없는 구간에서 사다리 첫 칸이 곧 답이다. 첫 칸의 배율이 현재 배율 이하이면 "지금 배율이 한계"라고 말하고 성장 배율을 지어내지 않는다. 장애를 주입한 조합에서도 이분 탐색과 일치한다 |
| P1-36 | 뒤처진 판정을 지금 판정처럼 말하지 않는다 | 경량 재계산으로 훑기가 한 배율 뒤처지는 동안, 그 값을 읽는 자리 전부가 그 사실을 말한다. 이중화 문장은 비우고, 자원별 예보는 뒤처진 값으로 표시하며, 노드의 단일 장애점 표식은 붙이지 않는다. 낡음 표시가 등급의 위험 색을 덮지 않는다 |
| P1-37 | 출력이 사용자의 조작 순서에 좌우되지 않는다 | 같은 장애 집합이면 켠 순서와 무관하게 같은 미확인 사유와 같은 이전 기록 순서를 낸다. 사유가 겹치면 고정 우선순위로 고른다 |
| P1-38 | 외삽할 수 없는 입력을 "초과 없음"과 같은 형태로 내지 않는다 | 배율 0에서 사다리는 빈 목록이 아니라 계산 불가 표시를 낸다. 화면이 그 표시를 읽어 침묵하지 않는다 |

**여기서 배운 것.** 회귀는 사용률이 정확히 1.0인 축에서 났다. 엔진은 초과를 `> 1 + EPSILON`으로 판정하는데 화면은 `> 현재 배율 + 1e-9`로 다음 칸을 골라, 두 여유 기준이 어긋난 틈으로 여유가 0인 설계가 "1.06배까지 견딘다"고 말했다. 21개 템플릿 1736개 조합 중 13개에서 재현됐고, `inline-lb` 템플릿 자신의 실습 버튼이 그 상태를 만든다. 무장애 설계만 도는 테스트는 이 조합을 만들 수 없었다.

**규칙.** 같은 사건을 두 곳에서 판정할 때는 여유 기준을 공유한다. 판정 경계를 화면이 다시 정의하면 엔진과 어긋난다. 그리고 경계 조건 테스트는 장애를 주입한 조합까지 돌린다.

### 5.7 이 릴리스가 걷어낸 대역

카탈로그 드롭다운이 프로필을 붙일 때 걸던 `conditionSelection: 'explicit-profile'`을 지웠다(`src/app.js`). 그 플래그는 조건도 워크로드 입력도 수락 경로도 없던 동안 화면이 숫자를 내게 하려고 둔 임시 통로였고, P1-26·P1-27·P1-39가 그 자리를 대신한다.

바뀐 것을 숨기지 않는다. 카탈로그 장비를 붙이고 워크로드 조건을 적지 않으면 이제 **모든 축이 미확인**이다. 예전에는 숫자가 나왔다. 그 숫자는 조건을 확인하지 않은 값이었으므로, 사라진 것이 아니라 처음부터 없던 것이다.

---

## 6. 하지 않는 것

### 6.1 검토했으나 이 개정에서 빼는 것

| 후보 | 빼는 이유 |
|---|---|
| 읽기 전용 임베드(URL 주입, iframe, postMessage) | 한계값의 출처와 적용 조건을 그리는 곳이 인스펙터 하나뿐이다. 캔버스만 내보내면 근거 없는 숫자가 근거 있는 숫자처럼 남의 문서에 박힌다. 순서 문제이기도 하다 — 요구 신호(2절 신호 2)가 아직 관측되지 않았다 |
| 방향별 링크 미터 | 엔진이 demand 부하를 요청 방향 홉에만 얹는다. 북–남 템플릿의 모든 링크가 역방향 0%로 찍히는데 이는 계산값이 아니라 모델링하지 않은 양이다. 0%로 그리면 원칙 위반이다. 링크 방향 분배를 엔진에 넣는 것이 선행한다 |
| 실측 잔차 병기 | 엔진의 bps는 preamble과 IFG를 포함한 wire rate인데 SNMP 카운터는 그렇지 않다. 64바이트 링크가 계산 100%·실측 76%로 찍히고, 그것을 보정하면 포화 링크가 정상으로 보인다. 조건이 구조화되기 전에는 비교 집단도 성립하지 않는다 |
| 플로우에서 수요 생성 | demand에 축별 유래 층이 없어 실측 유래 표식을 실을 자리가 없다. NetFlow 플로우 수에서 초당 세션을 만드는 것은 active timeout과 단방향 이중 계수를 모르는 금지 파생이다 |
| 운영 상태 주입(SNMP로 DOWN 표시) | 훑기의 무장애 기준선 계약과 충돌한다. 덮이지 않은 자원이 정상으로 남아 현재 상태 기준이라는 표기가 안전으로 위장된다 |
| 시간축 재생, 프레임 스크러버, 카오스 런 | Phase 2(P2-1, P2-5). 정상 상태 두 장의 비교로 답할 수 있는 질문을 먼저 소진한다 |

### 6.2 중단하는 작업

- 아이콘과 심볼 반복. 최근 73커밋 중 8커밋이 여기 있었다.
- 리뷰 스크린샷 갱신. 최근 60커밋 중 45커밋이 여기 있었다. 갱신 시점을 릴리스 단위로 묶는다.
- 도형 편집기 장식 확장(주석 도형, 정렬 모드, 커넥터 종류, 경유점 UI). 예외는 P1-29뿐이다.
- 합성 템플릿 추가. 현재 21개에서 멈춘다.
- 측정 조건을 전 장비에 채우기. G3의 30종만 채우고 나머지는 미확인으로 둔다.
- 텔레메트리 잔재 정리와 그에 딸린 문서 개정. P1-31, P1-32만 예외였다.

### 6.3 미결

차단이던 넷은 답이 나왔다. 10절에 결정으로 옮겼다. 아래는 진행하면서 정해도 되는 것들이다.

| # | 질문 | 영향 |
|---|---|---|
| Q1 | 게이트 시작일과 `docs/pilot-log.md`의 형식·기록 주체 | G1–G4 |
| Q2 | 90일 판정의 사용 사례 기준. '세 팀'은 90일 안에 셀 수 없다. 본인이 실제 설계 검토 한 건에 첨부했는가로 낮추는가 | 11절 |
| Q3 | `DESIGN.md`가 정본인가. 합성 텔레메트리 규정과 내부 불일치 두 건을 코드 쪽으로 고치는가 | 문서 |
| Q4 | 축 단위 미확인 사유를 어휘로 나눌 것인가. 지금은 축 사유와 이전 기록 사유가 서로 다른 어휘를 쓴다 | P1-21 |
| Q5 | 성장 사다리에 경고 임계 입력을 넣는가. 넣으면 저장 경로와 범위 검증이 따라온다 | P1-20 |
| Q6 | 덮어쓴 축의 적용 판정 시점. 계산 시점 재판정으로 바꾸면 `device.limits`의 뜻이 실효값에서 적용 후보값으로 바뀐다 | P1-28 |
| Q7 | 템플릿에서 시작해 편집한 설계의 합성 표시. 값의 출처가 합성 템플릿이라는 해석을 유지하는가 | P1-33 |
| Q8 | demand 방향의 의미. 출발지에서 목적지가 요청 방향인가 바이트 방향인가 | 6.1의 방향별 미터와 실측 잔차 |
| Q9 | HA 그룹 편집 UI를 Phase 1에 넣는가. 넣지 않으면 P1-21이 템플릿 한 개에서만 보인다 | P1-21 |
| Q10 | P1-29와 P1-30의 순서 | 5.4 |
| Q11 | 브라우저 스모크를 게이트에 넣는 방법. 지금은 단위 테스트만 기본 명령이 잡는다 | 8절 |

v0.5 13절의 미결(이름 가용성, 라이선스, 성능 데이터 수집 방식, 경로 입력 방식, 전력 기준)은 그대로 남는다. v0.5의 **웹 임베딩** 미결은 6.1에 따라 이 개정에서 보류로 결론낸다.

---

## 7. 원칙

v0.5의 원칙을 이어받고 둘을 강화한다.

**모르는 값을 어느 방향으로도 지어내지 않는다.** v0.5 10절은 "안전 상태로 바꾸지 않는다"고 적었다. 이번에 발견한 위반은 한쪽이 안전(사용률 0%), 다른 쪽이 위험(headroom 0)이었다. 방향과 무관하게 금지한다. 렌더 경로에서 미확인 값을 기본값으로 채우지 않는다. 미확인 계열은 선을 그리지 않고, 그려 둔 값과 이력도 남기지 않는다. 미확인 구간을 건너뛴 추세선은 없던 사실을 그린 그림이다.

**출력은 조작 순서에 좌우되지 않는다.** v0.5 10절의 결정성은 "같은 입력은 같은 출력"이다. 화면이 사용자의 클릭 순서를 담은 집합을 그대로 계산에 넘기면 같은 장애 집합이 다른 결과를 낸다. 계산에 들어가는 집합은 정렬해서 넘기고, 값이 겹칠 때 무엇을 남길지는 고정 우선순위로 정한다.

**계산 모델을 이름으로 밝힌다.** 성장 사다리는 부하가 배율에 선형이라는 성질에만 기댄다. 이 가정을 `linear-offered-load`라는 이름으로 결과에 싣고, 테스트가 모든 템플릿에서 선형성을 고정한다. 가정이 깨지는 입력이 생기면 모델명을 바꾸거나 그 축을 사다리에서 뺀다. 이름 없는 가정은 검증할 수 없다.

---

## 8. 검증

v0.5 9절에 더한다.

**미확인 표본 금지.** 한계를 모르는 설계에서 재계산해도 미확인 계열의 표본 수가 늘지 않는다. 그려 둔 경로 데이터가 남지 않는다.

**미확인 사유 구분.** 재수립 창 미선언과 동시 세션 미선언이 서로 다른 사유로 기록된다. 선언된 0과 경로가 겹치지 않은 경우는 미확인이 아니다.

**모델 대조.** 성장 사다리의 첫 항목이 이분 탐색 결과와 모든 템플릿·세 배율에서 0.011 이내로 일치한다. 배율 k를 곱하면 아는 축의 사용률이 정확히 k배가 된다.

**표기 정합.** 화면의 제목, 합성 표기, 계산 노트가 열려 있는 설계와 엔진 상수에서 온다. 사용자 설계에서 합성 표기가 나오지 않는다.

**경계 조건은 장애를 주입해 검사한다.** 사용률이 정확히 한계에 닿는 조합은 무장애 설계에서 잘 나오지 않는다. 사다리 대조는 무장애, 단일 장비 장애, 단일 링크 장애를 모두 돈다.

**조작 순서 불변.** 같은 장애 집합을 다른 순서로 켠 두 결과의 미확인 사유와 이전 기록이 같다.

**스모크의 위치.** 브라우저 흐름 검사가 기본 테스트 명령에 포함되는지 정한다(Q11). 지금은 별도 명령으로만 돈다.

**조건 어휘 세 값.** 구체값은 대조하고, `unknown`은 미확인으로 남기고, `not_applicable`은 대조하지 않는다. 빈 목록은 값이지 부재가 아니다 — 지워 버리면 방화벽만 켠 프로필과 맞출 수 있는 근거를 영영 못 맞춘다.

**수락은 스스로 풀린다.** 워크로드 조건이나 프로필이 바뀌면 그 조건 아래에서 한 수락이 남지 않는다. 저장 파일에 형식만 맞는 digest를 적어 넣어도 계산이 인정하지 않는다.

**변경을 적대적으로 검토한다.** 원칙에 닿는 변경은 넣은 뒤 여러 렌즈로 반박하고, 지적마다 재현자와 반박자가 코드를 실행해 확인한다. 이 개정의 회귀는 그 절차가 잡았다.

---

## 9. 비기능 요구사항

v0.5 10절의 표를 유지하고 재계산 항목을 갱신한다.

| 항목 | 목표 |
|---|---|
| 재계산 지연 | 상호작용 종류로 나눈다. 배율을 끄는 동안은 시나리오 계산만 돌고, 손을 뗀 뒤 한 번 전체 경로가 돈다. 전체 경로는 v0.5의 p95 100ms 목표를 그대로 따른다 |
| 재계산 비용 | 화면이 같은 답을 얻으려고 시나리오를 여러 번 다시 계산하지 않는다. 닫힌 계산으로 낼 수 있는 값은 엔진이 결과에 싣는다 |
| 결과 크기 | 결과에 실리는 파생 목록이 자원 수에 선형을 넘지 않는다. 자원별·축별 기여처럼 곱으로 커지는 값은 옵션일 때만 싣고 기본 내보내기에 넣지 않는다 |

기준 코드에서 잰 값이다. 합성 데모 설계, 재계산 1회당.

| | 시간 |
|---|---|
| 이전 (시나리오 + 훑기 + 이분 탐색 20회) | 2.89 ms |
| 이후 전체 (시나리오 + 훑기) | 1.02 ms |
| 이후 경량 (시나리오만) | 0.05 ms |

v0.5 10절이 정한 기준 브라우저와 하드웨어는 여전히 미정이다. 위 수치는 상대 비교로만 쓴다.

---

## 10. 결정 사항

**본체는 용량 산정이다.** 살아있는 다이어그램은 표면, 교육 템플릿은 사례집이다. 2위는 확정하지 않는다.

**종료 게이트는 코드가 도달한 상태로 판정한다.** 외부 사용 사례와 축별 오차는 기록만 시작하고 다음 분기 지표로 둔다.

**성장 한계는 닫힌 계산으로 낸다.** 부하가 배율에 선형이라는 성질을 `linear-offered-load`로 이름 붙여 결과에 싣는다. 화면이 이분 탐색으로 시나리오를 다시 계산하지 않는다.

**미확인은 어느 방향으로도 채우지 않는다.** 안전으로도 위험으로도 바꾸지 않는다.

**문서 임베딩은 보류한다.** 근거층이 임베드에 실리는 구조가 생기고, 요구 신호가 관측된 뒤 다시 올린다.

**측정 조건은 축 단위로 붙인다.** 한 장비의 축마다 측정 조건이 다르다. FortiGate 100F만 봐도 처리량은 1518바이트 기준, CPS는 TCP 기준, 동시 세션은 조건이 없다. 프로필 하나에 조건 하나를 씌우면 셋 중 둘이 틀린다. G3의 30은 축 레코드 30개이며 장비로는 약 8종이다.

**측정 조건 키는 v0.5 7.2절 어휘를 쓴다.** `packet_size_bytes`, `cipher`, `features_enabled`. `src/device-import.js`와 그 테스트가 이미 이 이름을 쓴다. v0.5 5.3절의 트래픽 프로파일은 `frame_bytes` 같은 자기 이름을 유지하고, 비교 시점에 조건 어휘로 매핑한다. 서로 다른 객체이므로 이름을 통일하지 않는다. `tests/solver-boundary.test.js`의 `packetBytes`는 픽스처이므로 함께 바꾼다.

**조건 대조는 정확 동등을 유지하고, 불일치는 축 단위 명시 수락으로 흡수한다.** 하한 판정 상태(`applicable-as-bound`)가 이론적으로 옳지만 v0.5 7.2 스키마 개정이 선행하므로 다음 개정으로 미룬다. 수락한 값은 `user-asserted`로 기록하고 조건이나 프로필이 바뀌면 풀린다(P1-39).

**보고서는 한 모델에서 세 형식으로 낸다.** 마크다운은 리뷰 본문에 붙이고, HTML은 첨부 파일로 내고, JSON은 기계가 읽는다. 숫자를 만드는 곳은 모델 하나뿐이며 렌더러는 그 모델만 읽는다. 형식마다 문장을 따로 쓰면 셋이 서로 다른 말을 하게 된다.

---

## 11. 성공 지표

v0.5 14절의 Alpha 종료 기준을 유지하고 이 개정의 판정 기준을 더한다.

**90일 판정.** 4절의 G1~G4. 넷 다 코드가 도달한 상태로 판정한다.

**다음 분기로 미룬 것.** 서로 다른 세 팀의 설계 리뷰 첨부, 조건 일치 실측 10건과 축별 오차, 외부 기여자의 성능 프로필 추가. `docs/pilot-log.md`에 기록을 시작한다. 90일 안에 셀 수 있는 사용 사례 기준은 6.3의 Q2로 남긴다.

---

## 부록. 이 개정의 근거

방향 판정과 요구사항 목록은 다음 과정에서 나왔다.

- 코드·PRD·화면·데이터·최근 궤적·인접 도구를 여섯 갈래로 읽었다.
- 네 방향 논제(용량 산정, 살아있는 다이어그램, 교육, 반대 논제)를 각각 옹호하고 세 렌즈(사용자 가치, 실현성, 차별성)로 채점했다.
- 여덟 관점에서 장치 후보 51개를 내고 16개로 묶은 뒤, 후보마다 실현성·적합성·차별성 세 검증자가 반박해 10개가 남았다.
- 완결성 비평이 1차 결과에서 누락 14건, 근거 없는 단정 7건, 자기모순 7건을 찾아 2차로 고쳤다.

수치 중 다음은 이 저장소에서 직접 실행해 확인했다.

| 확인한 것 | 값 |
|---|---|
| 배율에 대한 부하 선형성 오차 | 21개 템플릿 287개 축 표본에서 2.2e-16 |
| 구조화된 측정 조건을 가진 프로필 | 75개 중 0개 |
| 카탈로그 장비를 붙인 뒤 계산되는 축 | 4개 중 0개 (FortiGate 100F · fw-1518) |
| 카탈로그 판 식별자 참조 | 0건 |
| 재계산마다 성장 한계 탐색이 더 돌던 시나리오 계산 | 최대 20회 |
| 사다리 첫 칸이 현재 배율보다 앞서는 경우 | 21개 템플릿 1045개 조합에서 0건 |

적대적 검토는 여섯 렌즈가 27건을 지적하고 지적마다 재현자·반박자 두 명이 검증해 10건이 확정됐다. 확정된 10건은 5.6절 또는 이 개정의 코드에 반영했다.

시장 조사(WAN 용량 계획기, 다이어그램 도구, 모니터링 맵)는 검증자 진술이며 세션 안에서 원문을 재확인하지 못했다. 2절의 시장 대조는 그 전제로 읽는다.
